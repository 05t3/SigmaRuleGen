services:
  sigmarulegen:
    image: yourdockerhubuser/sigmarulegen:1.0.0
    container_name: sigmarulegen
    env_file:
      - .env
    ports:
      - "${PORT}:${PORT}" # uses PORT from .env
    restart: unless-stopped
