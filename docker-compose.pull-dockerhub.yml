services:
  sigmarulegen:
    image: yourdockerhubuser/sigmarulegen:latest
    container_name: sigmarulegen
    env_file:
      - .env
    ports:
      - "${PORT}:${PORT}" # uses PORT from .env
    restart: unless-stopped
