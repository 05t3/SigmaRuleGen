# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.

# A sample workflow which checks out the code, builds a container
# image using Docker and scans that image for vulnerabilities using
# Snyk. The results are then uploaded to GitHub Security Code Scanning
#
# For more examples, including how to limit scans to only high-severity
# issues, monitor images for newly disclosed vulnerabilities in Snyk and
# fail PR checks for new vulnerabilities, see https://github.com/snyk/actions/

name: Snyk Container Scan

on:
  workflow_dispatch:
  push:
    branches: ["main"]

permissions:
  contents: read
  security-events: write
  actions: read

env:
  DOCKERHUB_IMAGE: oste/sigmarulegen:1.0.0
  GHCR_IMAGE: ghcr.io/oste/sigmarulegen:1.0.0

jobs:
  snyk:
    name: Snyk Scan - DockerHub & GHCR
    runs-on: ubuntu-latest

    steps:
      - name: ⬇️ Checkout Repository
        uses: actions/checkout@v4

      - name: 📄 Read version from VERSION file
        id: version
        run: echo "version=$(cat VERSION)" >> "$GITHUB_OUTPUT"

      - name: 🔐 Docker Login (DockerHub)
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: 🔐 Docker Login (GHCR)
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: 🧰 Install Snyk CLI
        run: npm install -g snyk

      - name: 🔐 Authenticate Snyk
        run: snyk auth ${{ secrets.SNYK_TOKEN }}

      # Pull and scan images
      - name: 📦 Pull GHCR Image
        id: ghcr_pull
        run: |
          IMAGE=ghcr.io/oste/sigmarulegen:${{ steps.version.outputs.version }}
          docker pull $IMAGE || echo "GHCR image not found"

      - name: 🧪 Scan GHCR Image with Snyk
        if: steps.ghcr_pull.outcome == 'success'
        run: |
          IMAGE=ghcr.io/oste/sigmarulegen:${{ steps.version.outputs.version }}
          snyk container test $IMAGE \
            --file=Dockerfile \
            --sarif-file-output=snyk-ghcr.sarif || echo "GHCR Snyk scan failed"

      - name: 📦 Pull DockerHub Image
        id: dockerhub_pull
        run: |
          IMAGE=oste/sigmarulegen:${{ steps.version.outputs.version }}
          docker pull $IMAGE || echo "DockerHub image not found"

      - name: 🧪 Scan DockerHub Image with Snyk
        if: steps.dockerhub_pull.outcome == 'success'
        run: |
          IMAGE=oste/sigmarulegen:${{ steps.version.outputs.version }}
          snyk container test $IMAGE \
            --file=Dockerfile \
            --sarif-file-output=snyk-dockerhub.sarif || echo "DockerHub Snyk scan failed"

      # Upload SARIFs
      - name: ☁️ Upload SARIF for GHCR
        if: steps.ghcr_pull.outcome == 'success' && hashFiles('snyk-ghcr.sarif') != ''
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: snyk-ghcr.sarif
          category: snyk-ghcr

      - name: ☁️ Upload SARIF for DockerHub
        if: steps.dockerhub_pull.outcome == 'success' && hashFiles('snyk-dockerhub.sarif') != ''
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: snyk-dockerhub.sarif
          category: snyk-dockerhub








      # - name: Run Snyk to check Docker image for vulnerabilities
      #   continue-on-error: true
      #   uses: snyk/actions/docker@14818c4695ecc4045f33c9cee9e795a788711ca4
      #   env:
      #     SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
      #   with:
      #     image: ghcr.io/${{ steps.image.outputs.name }}:${{ steps.get_version.outputs.version }}
      #     args: >
      #       --file=Dockerfile
      #       --sarif-file-output=snyk.sarif
      #       --exclude-base-image-vulns


# If in the future you want to scan multiple targets (e.g., Docker image + dependencies), you must:

# Run each Snyk scan separately

# Output to a separate SARIF file, e.g.:

# snyk-docker.sarif

# snyk-code.sarif

# Upload each with a separate upload-sarif step:

# - name: Upload Docker Scan Results
#   uses: github/codeql-action/upload-sarif@v3
#   with:
#     sarif_file: snyk-docker.sarif

# - name: Upload Code Scan Results
#   uses: github/codeql-action/upload-sarif@v3
#   with:
#     sarif_file: snyk-code.sarif