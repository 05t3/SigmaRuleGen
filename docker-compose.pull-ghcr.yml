services:
  sigmarulegen:
    image: ghcr.io/05t3/sigmarulegen:latest
    ports:
      - "${PORT}:${PORT}" # uses PORT from .env
    container_name: sigmarulegen
    restart: unless-stopped
    command: npm run start